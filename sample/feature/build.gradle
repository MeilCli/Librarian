import net.meilcli.librarian.gradle.dependencies.SquareUp

apply plugin: 'com.android.dynamic-feature'
apply plugin: 'AndroidDynamicFeaturePlugin'  // this is define at buildSrc

dependencies {
    implementation project(':sample:sample-dynamic-app')
    implementation SquareUp.moshi
}
